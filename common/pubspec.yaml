name: 'dslideshow_common'
version: 2.0.0
description: An absolute bare-bones web app.

environment:
  sdk: '^3.2.0'

dependencies:
  built_value: ^8.8.0
  logging: ^1.0.0
  uuid: ^4.2.0

dev_dependencies:
  built_value_generator: ^8.8.0
  build_runner: ^2.4.6
#  test: ^1.3.0
